{
  "name": "vocx-dashboard-frontend",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "./dashboard",
    "./contacts",
    "./news",
    "./todo"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run --scope=nested_app* build",
    "serve": "lerna run --scope=nested_app* --parallel serve",
    "clean": "lerna run --scope=nested_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}