{
  "name": "vocx-dashboard-frontend",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "./dashboard",
    "./contacts",
    "./news",
    "./todo"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}